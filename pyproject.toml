[project]
name = "topic4-arag-eval"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.12",
]
